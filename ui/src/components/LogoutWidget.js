// src/components/LogoutWidget.js
import React from 'react';

const LogoutWidget = () => {
    return (
        <div className="overflow-hidden">
            <h4>Logout Widget</h4>
            <p>Contenu du widget Logout...</p>
        </div>
    );
};

export default LogoutWidget;
